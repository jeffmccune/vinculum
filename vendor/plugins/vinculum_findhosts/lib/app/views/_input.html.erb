<div class="ui-widget-header input-header">
    Filter parameters
</div>

<table id="input-table">
    <tr>
        <td>Filter by fact</td>
        <td><%= text_field_tag "fact", "",:size=>20, :id=>"fact"%></td>
    </tr>

    <tr>
        <td>Filter by class</td>
        <td><%= text_field_tag "pclass", "",:size=>20, :id=>"pclass"%></td>
    </tr>
    <tr>
        <td>Filter by identity</td>
        <td><%= text_field_tag "identity", "",:size=>20, :id=>"identity"%></td>
    </tr>
</table>

<div class="input-footer">
    - Fact like country=uk<br>
    - Multiple Facts like country=uk location=hetzner<br>
    - Class like /dev_server/ <br>
</div>

<div class="input-buttons">
    <%= button_to_remote "Submit", {:url=>{:controller => :findhosts, :action => :do_findhosts}, 
        :with => "'fact=' + encodeURIComponent(jQuery('#fact').val()) + '&pclass=' + encodeURIComponent(jQuery('#pclass').val()) + '&identity=' + encodeURIComponent(jQuery('#identity').val()) "}, :class => "button fg-button ui-state-default ui-corner-all input-button" %>
<%= button_to_function "Cancel", "closePopup();", :class => "button fg-button ui-state-default ui-corner-all input-button" %><
</div>
